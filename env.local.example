# Set to "true" to route Budgets, Accounts, Categories, and Transactions
# through the Go backend API instead of local IndexedDB storage.
NEXT_PUBLIC_USE_BACKEND=false

# Base URL for the Go backend API (defaults to http://localhost:8080).
NEXT_PUBLIC_API_URL=http://localhost:8080
